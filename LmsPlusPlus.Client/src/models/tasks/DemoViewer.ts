import { ObservableObject } from "../../ObservableObject"
import * as domain from "../../domain"

export class DemoViewer extends ObservableObject {
    constructor(task: domain.Task) {
        super()
    }
}
